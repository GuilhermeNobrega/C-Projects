
#include <stdio.h>
#include <string.h>
int tamanho(char changes[20]){
    printf("Vamos analisar:\n");
    int frase = strlen(changes);
    printf("Tamanho da frase solicitada eh %d\n", frase);
    }
int main()
{   int sla;
    printf("Vamos tentar executar essa funcao..\n");
    sla = tamanho("abcd");
}
